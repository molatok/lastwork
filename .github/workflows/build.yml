name: Build and Publish Docker image

on: [push]

jobs:
  build-and-publish:
    runs-on: ubuntu-latest
    steps:
      - name: list dir
        run: ls -la

      - name: clone code
        uses: actions/checkout@v2

      - name: list dir 2
          run: ls -la